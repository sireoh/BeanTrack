<div class="d-flex justify-content-end">
    <a href="/" class="me-auto mt-2 text-decoration-none fw-bold fs-5">🫛BeanTrack</a>
    <% if (typeof authenticated !== "undefined") { %>
        <% if (typeof username !== "undefined") { %>
            <div class="mt-2 me-2">Hello <span class="fw-bold"><%= username %></span>!</div>
            <a href="/tvlist/<%= username %>" class="btn btn-primary rounded-0">TV List</a>
            <a href="/movielist/<%= username %>" class="btn btn-primary rounded-0">Movie List</a>
        <% } %>
        <a href="/logout" class="btn btn-danger rounded-0">Logout</a>
    <% } else { %>
        <a href="/login" class="btn btn-dark rounded-0">Login</a>
        <a href="/signup" class="btn btn-secondary rounded-0">Sign Up</a>
    <% } %>
</div>